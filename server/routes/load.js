const express = require('express');
const dotenv = require('dotenv');
const { Connection } = require('@salesforce/core/lib/connection');
const router = express.Router();

router.get('/', (request, response, next) => {

});

module.exports = router;